// Temporal Dead Zone is from assign to until initialization

// Hoisting is the mechanism where variable and functions declarations are moved to top of the scope.

// var can redeclare and reassign;

// let can reassign but can't redeclare;

// const can't redeclare and can't reassign;