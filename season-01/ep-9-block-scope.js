{
    var a = 10;
    let b = 20;
    const c = 30;
    // a are stored one Global memory and b, a are stored in Block memory space.
}

// outside of the block of scope, we only access the global variables;



// Shadowing
// Global variable are overwritten called as shadowing.

// Global memory space is common;
// Block memory space is only for block of code;
// Script memory space is outside of the block;